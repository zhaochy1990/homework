# 媒体存储：COS 私有桶 + 临时凭证直传

图片与视频（学习资料、打卡视频）存腾讯云 COS 私有桶；Go 后端只签发临时访问凭证/预签名 URL，小程序直传直连，媒体流不经过后端。v1 不引入 CDN（流量小，COS 直链支持 Range 拖动播放）。否决的替代：后端中转上传（视频大，浪费后端带宽与内存）、微信云开发存储（与自建后端体系割裂，且未来 iOS App 无法复用）。部署环境已是腾讯云（与 auth-service 同环境），COS 是默认选择。
